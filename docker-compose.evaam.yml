version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: evaam_frontend
    ports:
      - "3001:3001"
    env_file:
      - .env
    restart: always
